node_modules/
.DS_Store
*.log
.env

# wrangler local state
.wrangler/

# wrangler secrets for local dev
.dev.vars

# rendered audio samples, not source
audio-samples/
.env.local

# rendered episodes on their way to R2, not source
audio-out/

# the built site. `npm run build` writes everything here from site/ plus courses/; the Pages
# workflow builds it in CI and publishes that, so none of it belongs in git. site/ is source.
dist/

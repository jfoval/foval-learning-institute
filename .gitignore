node_modules/
.DS_Store
*.log
.env

# wrangler local state
.wrangler/

# wrangler secrets for local dev
.dev.vars

# rendered audio samples, not source
audio-samples/
.env.local

# rendered episodes on their way to R2, not source
audio-out/

# static reading copies of every course and lesson, written by `npm run build` at deploy
site/courses/
site/sitemap.xml
